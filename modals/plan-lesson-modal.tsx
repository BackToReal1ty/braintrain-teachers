import { useState, FC, ChangeEvent, useContext, useEffect } from "react";

import { Spin, Form, message } from "antd";
import Image from "next/image";

interface ComponentProps {
  onCancel: () => void;
}

const PlanLessonModal: FC<ComponentProps> = ({ onCancel }) => {
  return <></>;
};

export default PlanLessonModal;
